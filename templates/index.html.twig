{% extends 'base.html.twig' %}
{% block body %}

    {% for post in posts %}
        <a href="{{ path('blog_show', {id: post.id, slug: post.slug}) }}">{{ post.title }}</a>
    {% endfor %}
{% endblock %}

