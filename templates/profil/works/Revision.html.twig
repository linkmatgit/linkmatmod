{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}

    <div class="container stack-large py5">
        <div class="flex">
            <div class="stack">
                <h1 class="hero-title"><strong>Proposer</strong><br> un changement</h1>

            </div>
            <div style="align-self: flex-end">
                <button class="btn-primary btn-big" type="submit" style="white-space: nowrap">Valider mes modifications</button>
            </div>
        </div>

        <div id="page-editor">
            {{ include('partials/flash.html.twig') }}
            {{ form_row(form.content, {label: false}) }}
        </div>

    </div>

    {{ form_end(form) }}

{% endblock %}