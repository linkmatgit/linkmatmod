doctrine:
    dbal:
        driver: pdo_sqlite
        memory: true
        charset: UTF8
        # "TEST_TOKEN" is typically set by ParaTest
        #dbname_suffix: '_test%env(default::TEST_TOKEN)%'
